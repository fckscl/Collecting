@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>
@{
    ViewBag.Title = Localizer["List"];
}

@*<a asp-action="Create">Добавить пользователя</a>*@

<table class="table">
    <tr><th>@Localizer["Email"] </th><th></th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            @*@if (user.UserRoles.Contains("active"))
            {
                <td>
                    <form asp-action="Block" asp-route-id="@user.UserId" method="post">
                        <button type="submit" class="btn btn-sm btn-danger">
                            @Localizer["Block"]
                        </button>
                    </form>
                </td>
            }
            else
            {
                <td>
                    <form asp-action="Block" asp-route-id="@user.UserId" method="post">
                        <button type="submit" class="btn btn-sm btn-primar">
                            @Localizer["Block"]
                        </button>
                    </form>
                </td>
            }*@
            <td>
                <form asp-action="Block" asp-route-id="@user.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-dark">
                        @Localizer["Block"]
                    </button>
                </form>
            </td>
            <td>
                <form asp-action="MakeAdmin" asp-route-id="@user.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-success">
                        @Localizer["MakeAdmin"]
                    </button>
                </form>
            </td>
            <td>
                <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        @Localizer["Delete"]
                    </button>
                </form>
            </td>
        </tr>
    }
</table>